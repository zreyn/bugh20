# How to bug-h20
```
./scripts/install.sh      # install python dependencies via poetry
./scripts/lint.sh .       # lint python code
./scripts/test.sh         # run tests (req 100% coverage)

./scripts/docker-push.sh  # build and push docker images
./scripts/deploy.sh       # run terraform
```